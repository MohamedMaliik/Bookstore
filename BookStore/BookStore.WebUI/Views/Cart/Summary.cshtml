@model BookStore.Domain.Entites.Cart
    

<div class="navbar-right hidden-xs">
    @Html.ActionLink("Checkout", "Index", "Cart", new { returnU = Request.Url.PathAndQuery },
     new{ @class= "btn btn-default navbar-btn" } );
</div>



<div class="navbar-right visible-xs">
  <a href="@Url.Action("Index","Cart"  ,
                            new { returnU = Request.Url.PathAndQuery })
     
                             class = "btn btn-default navbar-btn">
      
      <span class=glyphicon-shopping-cart></span> 
  </a>
</div>

<div class="navbar-text navbar-right">
    <b class="hidden-xs"> Your Cart:</b>
  @Model.lines.Sum(b => b.Quantity) book(s),
    @Model.computeTotal().ToString("c")

</div>
